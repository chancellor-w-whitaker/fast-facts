const [height, aspectRatio] = [38, 250 / 81];

const [intrinsicHeight, intrinsicWidth] = [162, 500];

export const ekuLogoDimensions = {
  width: height * aspectRatio,
  intrinsicHeight,
  intrinsicWidth,
  aspectRatio,
  height,
};
